<div class="col-md-1"></div>
<div class="container">
  <div class="col-md-10" id="show-model-container">
    <p id="notice" class="alert-success"><%= notice %></p>
    This grade was created on <%= @grade.created_at.strftime('%A, %d %B %Y %H:%M:%S') %>
    <p>Student: <%= @grade.student.name %></p>

    <p>Course: <%= @grade.course.name %></p>
    <%= link_to 'Back', grades_path, class: "btn btn-info pull-right" %>
  </div>
</div>
<div class="col-md-1"></div>


